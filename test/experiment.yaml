apiVersion: turing.touchturing.com/v1
kind: Experiment
metadata:
  name: myapp
  namespace: test
spec:
  image: nginx
  host: test.com
  port:
    name: web
    hostPort: 80
    containerPort: 80
  probe:
    path: /
    port: 80

